{
    "description": "Details of a group",
    "properties": {
        "_id": {
            "bsonType": "objectId"
        },
        "group_name": {
            "bsonType": "string",
            "description": "must be a string and is required",
            "uniqueItems": true
        },
        "group_cover_img_url": {
            "bsonType": "string",
            "description": "must be a string and is required"
        },
        "group_admin": {
            "bsonType": "objectId",
            "description": "must be an objectId and is required",
            "items": {
                "bsonType": "objectId"
            },
            "minItems": "1"
        },
        "group_location": {
            "bsonType": "string",
            "description": "must be a string and is required"
        },
        "group_description": {
            "bsonType": "string",
            "description": "must be a string and is required"
        },
        "members": {
            "bsonType": "array",
            "description": "must be an array and can be empty",
            "items": {
                "bsonType": "array"
            },
            "minItems": "0"
        }
    },
    "required": ["_id", "group_name", "group_cover_img_url", "group_admin", "group_location", "group_description"],
    "title": "groups"
}